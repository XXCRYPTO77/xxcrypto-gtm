# Airy 任务包 · A1.3 — ComingSoon 标签墙

**优先级**：P0（并行 A1.1 + A1.2）  
**预估工时**：1h  
**分支**：`feat/airy-a1.3-comingsoon`  

---

## 任务目标

按 v2 spec §2.6 重做 `src/modules/landing/components/ComingSoon.tsx`：
- P1-P9 标签墙，灰色调
- 明确 "Coming Soon" 标签
- 数据抽离到 `modules/landing/data/comingSoon.ts`
- 视觉权重最低（不抢 CapabilityTabs 的风头）

---

## P1-P9 内容

| id | 标签文字 | 说明（不出现在 UI，仅供你参考写数据） |
|----|----------|--------------------------------------|
| P1 | Telegram 通知 | Agent 通过 TG 推送交易通知 |
| P2 | Email 提醒 | 重要行情或订单状态发邮件 |
| P3 | API Key 管理 | 站内生成/吊销 Key，粒度控制 |
| P4 | 市场总览仪表盘 | 多维度行情数据聚合页 |
| P5 | 链上数据 | 巨鲸动向、大额转账 |
| P6 | 加密新闻聚合 | 实时热点和市场情绪 |
| P7 | 大额确认弹窗 | 超阈值订单需二次确认 |
| P8 | AI 交易报告 | 周期性 AI 生成持仓分析 |
| P9 | 风险评分 | 每笔交易前 AI 评估风险 |

---

## 接口定义

```typescript
// 追加到 src/modules/landing/data/types.ts
export interface ComingSoonItem {
  id: string;
  label: string;   // 标签文字，直接显示
}
```

---

## 组件设计要求

```
┌─────────────────────────────────────────────────┐
│  Coming Soon                         [灰色小标题] │
│                                                   │
│  [Telegram 通知] [Email 提醒] [API Key 管理]      │
│  [市场总览仪表盘] [链上数据] [加密新闻聚合]        │
│  [大额确认弹窗] [AI 交易报告] [风险评分]           │
│                                                   │
│  更多能力持续开放中                    [灰色副文字] │
└─────────────────────────────────────────────────┘
```

**样式要求**：
- 背景：`bg-gray-50` 或 `bg-muted/30`
- 标签：`border border-border text-muted rounded-full px-3 py-1 text-sm`，不加颜色
- 标题 "Coming Soon"：小字灰色，不要用 h2，用 `text-sm text-muted font-medium uppercase tracking-wider`
- 整个 section `py-8`，节省垂直空间（比 CapabilityTabs 的 `py-10` 小）
- 不要 hover 效果，不可点击，就是展示

---

## 文件输出

**必须创建/修改**：
1. `src/modules/landing/data/types.ts` — 追加 `ComingSoonItem`
2. `src/modules/landing/data/comingSoon.ts` — 9 条数据
3. `src/modules/landing/components/ComingSoon.tsx` — 重写（当前是 42 行，直接替换）

---

## 不要做的事

- ❌ 不要把 P1-P9 做成可点击的（没有跳转目标）
- ❌ 不要加动画（视觉权重最低原则）
- ❌ 不要动 i18n 文件
- ❌ 不要改 index.tsx（F 来接入）

---

## 交付方式

```bash
git checkout master && git pull
git checkout -b feat/airy-a1.3-comingsoon
# 建/改文件
git add src/modules/landing/data/ src/modules/landing/components/ComingSoon.tsx
git commit -m "feat(landing): P1-P9 coming soon tag wall per v2 spec"
git push -u origin feat/airy-a1.3-comingsoon
```

push 完告诉 Dan commit hash。
